{
	"name": "blog",
	"version": "0.1.0",
	"private": false,
	"homepage": "https://devtsp.github.io/blog",
	"dependencies": {
		"axios": "^0.24.0",
		"date-fns": "^2.27.0",
		"easy-peasy": "^5.0.4",
		"react": "^17.0.2",
		"react-dom": "^17.0.2",
		"react-icons": "^4.3.1",
		"react-router-dom": "^6.2.1",
		"react-scripts": "^5.0.0"
	},
	"scripts": {
		"server": "npx json-server --watch data/db.json --port 3500",
		"start": "react-scripts start",
		"launchall": "start npm run server && start npm run start",
		"build": "react-scripts build",
		"test": "react-scripts test",
		"eject": "react-scripts eject",
		"predeploy": "npm run build",
		"deploy": "gh-pages -d build"
	},
	"eslintConfig": {
		"extends": [
			"react-app",
			"react-app/jest"
		]
	},
	"browserslist": {
		"production": [
			">0.2%",
			"not dead",
			"not op_mini all"
		],
		"development": [
			"last 1 chrome version",
			"last 1 firefox version",
			"last 1 safari version"
		]
	},
	"devDependencies": {
		"gh-pages": "^3.2.3"
	}
}
